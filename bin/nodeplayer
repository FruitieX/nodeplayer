#!/usr/bin/env node

var Core = require('../');
var labeledLogger = require('../logger');

var logger = labeledLogger('core');

var core = new Core();
core.initModules(function() {
    logger.info('ready');
});
